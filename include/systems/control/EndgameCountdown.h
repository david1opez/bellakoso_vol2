#include "main.h"

#ifndef __ENDGAME_COUNTDOWN_H__
#define __ENDGAME_COUNTDOWN_H__

/**
 * @brief Counts down to the endgame of the match and if the time is less than 10 seconds, it will rumble the controller.
 * 
 * @param countdown The time left in the match.
 * 
 * @return int The time left in the match.
 */

int EndgameCountdown(int countdown);

#endif // __ENDGAME_COUNTDOWN_H__